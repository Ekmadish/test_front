import 'package:flutter/material.dart';

class MeasurementnameScreen extends StatelessWidget {
  const MeasurementnameScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return Container(
      alignment: Alignment.center,
      child: Text('MeasurementnameScreen'),
    );
  }
}
