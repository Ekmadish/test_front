import 'package:flutter/material.dart';

class FederaldistrictScreen extends StatelessWidget {
  const FederaldistrictScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  return Container(
      alignment: Alignment.center,
      child: Text('FederaldistrictScreen'),
    );
  }
}
