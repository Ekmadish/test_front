import 'package:flutter/material.dart';

class TripstatusesScreen extends StatelessWidget {
  const TripstatusesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   return Container(
      alignment: Alignment.center,
      child: Text('TripstatusesScreen'),
    );
  }
}
