import 'package:flutter/material.dart';

class ContacttypesScreen extends StatelessWidget {
  const ContacttypesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   return Container(
      alignment: Alignment.center,
      child: Text('ContacttypesScreen'),
    );
  }
}
