import 'package:flutter/material.dart';

class LoadingmethodScreen extends StatelessWidget {
  const LoadingmethodScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return Container(
      alignment: Alignment.center,
      child: Text('LoadingmethodScreen'),
    );
  }
}
