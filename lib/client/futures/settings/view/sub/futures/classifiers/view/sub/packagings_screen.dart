import 'package:flutter/material.dart';

class PackagingsScreen extends StatelessWidget {
  const PackagingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
return Container(
      alignment: Alignment.center,
      child: Text('PackagingsScreen'),
    );
  }
}
