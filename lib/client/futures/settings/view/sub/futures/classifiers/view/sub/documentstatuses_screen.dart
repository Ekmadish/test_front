import 'package:flutter/material.dart';

class DocumentstatusesScreen extends StatelessWidget {
  const DocumentstatusesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('DocumentstatusesScreen'),
    );
  }
}
