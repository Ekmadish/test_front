import 'package:flutter/material.dart';

class VatratesScreen extends StatelessWidget {
  const VatratesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return Container(
      alignment: Alignment.center,
      child: Text('VatratesScreen'),
    );
  }
}
