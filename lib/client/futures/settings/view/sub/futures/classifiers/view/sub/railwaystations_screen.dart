import 'package:flutter/material.dart';

class RailwaystationsScreen extends StatelessWidget {
  const RailwaystationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  return Container(
      alignment: Alignment.center,
      child: Text('RailwaystationsScreen'),
    );
  }
}
