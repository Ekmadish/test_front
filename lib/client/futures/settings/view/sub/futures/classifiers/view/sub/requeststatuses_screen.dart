import 'package:flutter/material.dart';

class RequeststatusesScreen extends StatelessWidget {
  const RequeststatusesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
return Container(
      alignment: Alignment.center,
      child: Text('RequeststatusesScreen'),
    );
  }
}
