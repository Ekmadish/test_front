import 'package:flutter/material.dart';

class FlexformpresetsScreen extends StatelessWidget {
  const FlexformpresetsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  return Container(
      alignment: Alignment.center,
      child: Text('FlexformpresetsScreen'),
    );
  }
}
