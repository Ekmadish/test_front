import 'package:flutter/material.dart';

class WorkerspostsScreen extends StatelessWidget {
  const WorkerspostsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('WorkerspostsScreen'),
    );
  }
}
