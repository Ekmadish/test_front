import 'package:flutter/widgets.dart';

class DrivinglicencecategoriesScreen extends StatelessWidget {
  const DrivinglicencecategoriesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
 return Container(
      alignment: Alignment.center,
      child: Text('DrivinglicencecategoriesScreen'),
    );
  }
}
