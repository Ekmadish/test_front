import 'package:flutter/material.dart';

class SalesfunnelstatusScreen extends StatelessWidget {
  const SalesfunnelstatusScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  return Container(
      alignment: Alignment.center,
      child: Text('SalesfunnelstatusScreen'),
    );
  }
}
