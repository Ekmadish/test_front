import 'package:flutter/material.dart';

class PersonCountScreen extends StatelessWidget {
  const PersonCountScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PersonCountScreen'),
      ),
    );
  }
}
